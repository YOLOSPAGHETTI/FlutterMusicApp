import 'package:flutter/material.dart';

ThemeData lightMode = ThemeData(
    colorScheme: ColorScheme.light(
        surface: const Color(0xFFFFFFFF), // background
        primary: Color(0xFF6200EE), // buttons, headers, navigation
        secondary: Color(0xFF03DAC5), // switches, sliders, icons
        inversePrimary: Color(0xFF3700B3), // other
        tertiary: Color(0xFF424242))); // text
